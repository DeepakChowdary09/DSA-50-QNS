// You are given an integer array nums
// and an integer k.
// Return the total number of continuous subarrays
// whose sum equals k.

// Formula:
// prefixSum[j] − prefixSum[i] = k
// → prefixSum[i] = prefixSum[j] − k

// Rules:
// - Subarray must be contiguous
// - Array can contain negative numbers
// - Return count (not length)

// Pattern
// Prefix Sum + HashMap (Frequency Count)

// When to Use (Trigger)
// - Count of subarrays with exact sum
// - Negative numbers present
// - Sliding window not reliable

// ─────────────────────────────────────────
// Core Invariant
// ─────────────────────────────────────────
// Maintain running prefix sum
// If (currentSum − k) exists in map,
// then that many subarrays end at current index

// Pointer / Calculation Logic
// ─────────────────────────────────────────
// - Initialize map with (0 → 1)
// - For each number:
// update running sum
// if (sum − k) exists → add its frequency to count
// store/update current sum frequency

// ─────────────────────────────────────────
// Time & Space
// ─────────────────────────────────────────
// Time: O(n)
// Space: O(n)
