Minimum Size Subarray Sum (LC 209)

// You are given an array of positive integers nums
// and a positive integer target.
// Return the minimal length of a contiguous subarray
// whose sum is greater than or equal to target.
// If no such subarray exists, return 0.

// Formula:
// windowSum >= target → valid window
// window length = right − left + 1

// Rules:
// - Subarray must be contiguous
// - All numbers are positive
// - Return minimum length

// Pattern
// Sliding Window (Variable Size)

/ When to Use (Trigger)
// - Contiguous subarray problems
// - Positive numbers only
// - Need minimum length satisfying condition

// ─────────────────────────────────────────
// Core Invariant
// ─────────────────────────────────────────
// Expand window to increase sum
// Shrink window while condition is satisfied
// Always try to minimize window length once valid

/ Pointer Rules (Measurement Logic)
// ─────────────────────────────────────────
// - Expand right pointer and add to sum
// - While sum >= target:
// update minimum length
// subtract nums[left]
// move left pointer

// ─────────────────────────────────────────
// Time & Space
// ─────────────────────────────────────────
// Time: O(n)
// Space: O(1)
